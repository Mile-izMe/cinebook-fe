export interface User {
  id: string;
  roleId: string;
  userName: string;
  email: string;
  phone: string;
  roleCode?: string;
  avatarUrl?: string;
}

export interface UserType {
  id: string;
  roleId: string;
  userName: string;
  email: string;
  phone: string;
  roleCode?: string;
  avatarUrl?: string;
}
